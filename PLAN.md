# 轻量化项目管理工具实施计划

## 概要

建设仅供公司内部员工使用的开源 Web 系统，采用 Vue 3 + Spring Boot + PostgreSQL 架构；首版支持本地运行与单台 Linux Docker 部署。按三期交付全部模块，资料、部署方案、实施留痕和问题闭环为主线。

安全默认：密码、Token 等凭据采用加密存储；仅项目负责人和管理员可解密查看，并记录审计日志。不会支持明文保存。

## 技术与运行架构

- 前端：Vue 3、TypeScript、Vite、Element Plus。
- 后端：Java 21、Spring Boot 3、Spring Security、MyBatis-Flex、Flyway。
- 数据库：PostgreSQL；缓存与任务锁：Valkey。
- 文件：开发环境使用本地挂载目录；生产使用 S3 兼容对象存储或服务器挂载卷，单文件最大 2GB、总量按 1TB 规划，支持分片上传与文件校验。
- AI：Ollama 以 Docker 方式与应用部署在同一台服务器、CPU 运行；使用可配置的本地模型完成日报润色与周报草稿生成。
- 飞书：本地账号登录；通过飞书自建应用读取已授权成员用于责任人选择，并通过飞书消息/群机器人通知。
- 部署：Docker Compose 管理前端、后端、PostgreSQL、Valkey、Ollama 和对象存储；Caddy 或 Nginx 负责 HTTPS 与反向代理。
- 开源协议：应用代码采用 Apache-2.0；第三方组件许可证单独在 `NOTICE` 中列明。

## 产品与架构参考策略

- 保留当前 Vue 3 + Spring Boot + PostgreSQL 应用作为唯一业务与数据底座，不将 Plane 或 OpenProject 变成第二套业务系统。
- 前端参考 Plane 的工作区、项目侧边栏、多视图切换、列表与详情联动等交互模式；界面使用 Vue 和 Element Plus 自主实现，不复制 Plane 源码、图标或品牌资源。
- 项目管理领域参考 OpenProject 的 Work Package 思路，将任务、需求、Bug、客户建议和里程碑逐步收敛为统一“工作项”，共享负责人、状态、优先级、日期、附件、关系和变更历史。
- 部署资产、服务器凭据、环境指纹、部署方案、日报周报等专业数据保持独立模型，通过显式关联与工作项连接，不塞入一张通用大表。
- 第一期继续完成已确认的资料与部署复用链路；交互壳层渐进调整，避免在当前模块开发中途全量重写。
- 完整决策背景、替代方案与后果见 `docs/decisions/ADR-001-retain-current-stack-and-reference-plane-openproject.md`。

## 核心数据与接口

### 基础能力

- `user`：账号、姓名、手机号/邮箱、角色、飞书 open_id、状态。
- `role`、`permission`：管理员、项目经理、实施人员、测试验证人员、访客。
- `project`：项目编码、名称、客户、负责人、状态、计划起止日期、标签、归档状态。
- `project_member`：项目、成员、项目内角色。
- `work_item`：统一工作项，类型包括任务、需求、Bug、客户建议和里程碑；包含标题、描述、状态、优先级、负责人、计划日期与实际日期。
- `work_item_relation`、`work_item_status_log`：父子、前置、阻塞等关系，以及可追溯的状态变更记录。
- `file_asset`：原始文件名、对象路径、大小、MIME、SHA-256、版本、上传人、关联业务类型与业务 ID。
- `audit_log`：操作者、时间、IP、动作、对象、变更摘要。
- 所有业务记录统一包含创建人、创建时间、更新人、更新时间、软删除时间。

### 项目资料、部署资产与环境复用

- `deployment_asset`：名称、类型（安装包/脚本/手册/配置模板/依赖组件）、版本、标签、适用系统、CPU 架构、依赖中间件、适用环境、风险等级、文件资产、说明。
- `deployment_solution`：方案名称、适用场景、环境指纹、部署架构、前置条件、部署步骤、回滚步骤、风险提示、状态。
- `deployment_solution_item`：部署方案与具体资产的关联、执行顺序、参数模板。
- `deployment_record`：项目、环境、目标服务器、所用部署方案、实际资产版本、执行人、执行时间、结果、异常说明。
- `environment_fingerprint`：操作系统、内核、架构、运行时版本、数据库、中间件、网络条件、标签。
- 资产版本不可覆盖；脚本必须填写前置条件、执行方式和回滚方案；只有关联成功部署记录的方案可标记为“成功部署基线”。
- 支持按操作系统、数据库、中间件、架构、部署类型、标签和“历史成功”条件检索，并展示对应问题、手册、脚本和配置模板。

### 服务器与系统信息

- `server_profile`：项目、环境、用途、堡垒机地址、服务器地址、端口、账号、操作系统、系统访问地址、数据库/中间件信息、凭据密文、密码库引用。
- 服务器资料可被部署记录、文档、接口和问题关联。
- 解密查看、复制、导出和编辑必须记录审计；复制项目时只复制脱敏模板，绝不复制凭据与历史附件。

### 技术问题、日报与周报

- `knowledge_article`：标题、场景、现象、原因、解决办法、关键词、适用条件、附件、审核状态。
- `daily_report`：日期、项目、原始工作内容、润色内容、工时、关联问题/会议/部署记录、确认状态。
- `weekly_report`：项目、周期、日报快照、周报内容、下周计划、生成方式、确认状态。
- AI 仅生成草稿；用户确认后才可提交日报或周报。周报生成后保存日报快照，后续日报修改不影响已发布周报。

### 厂商接口、会议与设计资料

- `vendor`：厂商名称、联系人、联系方式、系统名称、备注。
- `integration_record`：厂商、项目、接口名称、方向、协议、地址、鉴权方式、字段说明、推送频率、联调状态、问题说明、附件。
- `meeting`：项目、类型（例会/汇报/培训）、主题、时间、地点/链接、参会人员、纪要、待办、材料附件。
- `design_asset`：项目、类型（原型/埋点图/设计说明等）、名称、阶段、版本、外链、附件、关联需求、说明。
- 厂商接口记录和会议记录提交时，后端强制校验至少存在一个附件；会议纪要为必填。

### 工作项、风险与决策

- 任务、新需求、测试 Bug、客户优化建议和里程碑使用统一 `work_item` 基础模型，再按类型配置字段和状态流程。
- 问题类工作项额外包含来源、通知对象、处理说明和验证结论；里程碑类工作项可参与父子和前置关系。
- `risk`：项目、风险描述、等级、概率、影响、触发条件、应对方案、责任人、状态。
- `decision_record`：项目、关联风险、决策内容、决策人、决策日期、附件、版本。
- 问题状态只允许“未处理 → 已处理 → 已验证”，可从已处理或已验证重新打开；关闭前必须有处理说明与验证结论。
- 高风险必须指定责任人与应对方案；决策记录仅允许追加修订，不可物理删除。

### 对外接口

- 统一使用 `/api/v1`，采用服务端 Session Cookie + CSRF、分页、参数校验和统一错误响应。
- 提供项目、资产、部署方案、服务器、问题、日报、周报、厂商接口、会议、设计资料、里程碑、风险的标准增删改查接口。
- 提供文件分片上传、下载鉴权、预览、版本查询接口。
- 提供日报润色、周报草稿生成、飞书通知重试、项目资料检索接口。
- 飞书凭据、Ollama 地址、加密主密钥、数据库账号等全部通过环境变量或部署密钥配置，不写入代码仓库。

## 三期实施与验收

### 第一期：项目基础与资料复用

- 实现登录、角色权限、项目与项目成员、审计日志。
- 实现统一文件管理、文档分类、安装包/脚本/手册/配置模板归档。
- 实现服务器档案、环境指纹、部署方案、部署记录与相似环境检索。
- 验收：可创建项目，上传版本化资产，关联服务器并形成成功部署基线；可按环境定位历史成功方案；无权用户无法查看凭据或下载未授权文件。

### 第二期：实施留痕与工作沉淀

- 建立统一工作项基础能力，先支持任务和里程碑，提供列表、详情、父子/前置关系和变更历史。
- 实现技术知识库、日报、Ollama 润色、周报快照汇总。
- 实现厂商与接口记录、会议培训、设计资料；落实附件强制校验。
- 实现项目里程碑日历与表格视图、临期与延期提醒。
- 验收：可从原始日报生成并确认正式日报和周报；厂商对接、会议记录缺附件无法提交；可查看项目全周期节点。

### 第三期：问题闭环、飞书协同与风险管控

- 在统一工作项上启用新需求、测试 Bug 和客户优化建议类型，实现登记、分派、状态机、验证、附件与关联关系。
- 接入飞书成员选择、个人消息和群机器人通知，增加通知日志与失败重试。
- 实现风险、应对、决策记录；将里程碑延期和高优先级问题接入通知。
- 验收：问题可完整闭环并通知责任人；飞书失败不会阻塞业务保存且可重试；风险与决策可追溯。

## 测试与交付标准

- 后端：权限、状态机、附件强制校验、凭据加密、部署方案版本、周报快照、通知重试的单元与集成测试。
- 前端：关键表单校验、权限菜单、文件上传、日历节点、问题状态流转的端到端测试。
- 部署：通过 Docker Compose 在开发机一键启动；在干净 Linux 服务器完成一次部署演练。
- 安全：验证敏感字段不出现在接口日志、数据库明文和前端默认展示中；验证未授权访问被拒绝。
- 备份：提供 PostgreSQL 日备份、文件存储备份、恢复演练说明。
- 文档：提供本地启动、生产部署、飞书配置、Ollama 配置、数据备份恢复和管理员操作手册。

## 已确认的默认前提

- 系统仅面向公司内部员工，不提供客户或厂商登录入口。
- 首版使用本地账号认证，飞书仅用于成员选择与消息通知。
- 首个生产环境为单台 Linux Docker 服务器。
- 文件按单文件 2GB、总量 1TB 以内规划。
- AI 使用同机 CPU 运行的 Ollama，本地生成日报和周报草稿。
- 首版按三期建设，但最终覆盖全部十个业务模块与部署资产复用能力。
